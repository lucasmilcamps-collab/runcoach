MONGODB_URL=mongodb://localhost:27017
MONGODB_DB_NAME=runcoach
JWT_SECRET=change-me-to-a-long-random-string
GARMIN_TOKEN_ENCRYPTION_KEY=change-me-fernet-key
CORS_ORIGINS=["http://localhost:8081","http://localhost:19006"]
# TEMPORAIRE — n'activer que le temps de mesurer la limite de requête entrante
# de l'hébergeur (voir le skill plan-generator, « Limite de requête entrante »).
# Ouvre GET /debug/slow, qui immobilise un worker pendant N secondes.
# ENABLE_DEBUG_SLOW=true
