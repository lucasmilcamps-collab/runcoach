fastapi>=0.115
uvicorn[standard]>=0.32
motor>=3.6
pydantic>=2.9
pydantic[email]>=2.9
pydantic-settings>=2.6
python-jose[cryptography]>=3.3
passlib>=1.7
bcrypt==4.0.1
# >=0.3 for the `workout` module (structured workout push); <0.4 because the
# workout target/condition constants it exposes are part of our wire format.
garminconnect>=0.3,<0.4
cryptography>=43.0
anthropic>=0.40
pywebpush>=2.0
